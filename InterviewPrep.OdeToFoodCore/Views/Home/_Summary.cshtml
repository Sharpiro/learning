@model Restaurant

<section>
    <h3>@Model.Name</h3>
    <div>
        @Model.Cuisine
    </div>
    <div>
        @Html.ActionLink("Details", "Details", new { id = Model.Id })
        @Html.ActionLink("Edit", "Edit", new { id = Model.Id })
    </div>
</section>